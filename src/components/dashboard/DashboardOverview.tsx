import type { DashboardData } from "@/server/dashboard/service";
export function DashboardOverview({ overview }: Readonly<{ overview: DashboardData }>) { const n = (x:number)=>x.toLocaleString("ko-KR"); return <div><h1>Dashboard</h1><section><h2>Free spendable</h2><p>{n(overview.freeSpendable)}</p><h2>Daily spendable</h2><p>{n(overview.dailySpendable)}</p></section><section><p>Income: {n(overview.income)}</p><p>Expense: {n(overview.expense)}</p><p>Budget usage: {n(overview.budgetUsage)}</p><p>Liquid assets: {n(overview.liquidAssets)}</p><p>Net worth: {n(overview.netWorth)}</p><p>Card outstanding: {n(overview.cardOutstanding)}</p><p>Savings goals: {overview.savingsGoals}</p><p>Upcoming events: {overview.upcomingEvents}</p></section></div>; }
